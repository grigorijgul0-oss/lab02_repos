// dod.cpp
#include "dod.h"
#include "var.h"

using namespace nsVar;

void nsDod::dod() {
    // Формула переходу від попереднього доданку до наступного для ряду Тейлора
    // R = x^2 * (2n - 1) / (2n + 1)
    // Враховуючи, що в циклі sum n збільшується перед викликом, 
    // для першого кроку n=1 формула дасть множник 1/3, що вірно (2x -> 2x^3/3)
    a *= x * x * (2.0 * n - 1) / (2.0 * n + 1);
}